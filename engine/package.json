{
  "name": "pixelheart",
  "version": "1.0.0",
  "description": "A basic WebGL app with TypeScript and React",
  "scripts": {
    "build": "pnpm --recursive build",
    "clean": "pnpm --recursive --no-bail clean",
    "watch:packages": "pnpm --recursive watch",
    "watch:root": "tsc --build --watch --preserveWatchOutput",
    "dev": "pnpm run build && run-p watch:*"
  },
  "keywords": [],
  "author": "Glenn Conner",
  "license": "MIT",
  "devDependencies": {
    "@fsouza/prettierd": "^0.24.2",
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.9.5"
  }
}
