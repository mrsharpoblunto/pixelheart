{
  "name": "Adventures",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "npx pixelheart run --game . --output ./www",
    "clean": "npx pixelheart clean --game . --output ./www",
    "build": "pnpm run clean && npx pixelheart build --game . --output ./www",
    "serve": "pnpm run build --serve",
    "link": "pnpm link --global @pixelheart/client && pnpm link --global @pixelheart/server && pnpm link --global @pixelheart/build && pnpm link --global @pixelheart/effects"
  },
  "author": "Glenn Conner",
  "license": "MIT",
  "devDependencies": {
    "@pixelheart/build": "link:../engine/packages/build",
    "@pixelheart/client": "link:../engine/packages/client",
    "@pixelheart/effects": "link:../engine/packages/effects",
    "@pixelheart/server": "link:../engine/packages/server",
    "@types/node": "^20.10.5",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-resizable-panels": "^1.0.3"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "sharp": "^0.32.4"
  }
}
