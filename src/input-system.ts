import {GameState} from './game';
import {RenderContext} from './game-runner';

export default function init(context: RenderContext, state: GameState, delta: number) {
}

export default function update(context: RenderContext, state: GameState, delta: number) {

}
